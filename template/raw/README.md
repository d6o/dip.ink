# raw/ — immutable sources

Original documents the operator drops here for ingestion (articles, papers, exported docs). Read-only for agents: read these, never modify them. Durable knowledge extracted from them lives in `wiki/`.
